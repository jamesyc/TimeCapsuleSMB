diff --git a/source3/rpc_server/srv_pipe_hnd.c b/source3/rpc_server/srv_pipe_hnd.c
--- a/source3/rpc_server/srv_pipe_hnd.c
+++ b/source3/rpc_server/srv_pipe_hnd.c
@@ -136,6 +136,10 @@ NTSTATUS np_wait_exists_recv(struct tevent_req *req)
 	return tevent_req_simple_recv_ntstatus(req);
 }
 
+#ifdef TC_SAMBA4X_EMBEDDED_SRVSVC
+#include "tc_embedded_srvsvc.c"
+#endif
+
 NTSTATUS np_open(TALLOC_CTX *mem_ctx, const char *name,
 		 const struct tsocket_address *remote_client_address,
 		 const struct tsocket_address *local_server_address,
@@ -147,6 +151,9 @@ NTSTATUS np_open(TALLOC_CTX *mem_ctx, const char *name,
 	struct fake_file_handle *handle;
 	struct npa_state *npa = NULL;
 	int ret;
+#ifdef TC_SAMBA4X_EMBEDDED_SRVSVC
+	NTSTATUS status;
+#endif
 
 	handle = talloc(mem_ctx, struct fake_file_handle);
 	if (handle == NULL) {
@@ -163,6 +170,28 @@ NTSTATUS np_open(TALLOC_CTX *mem_ctx, const char *name,
 		.private_data = npa,
 	};
 
+#ifdef TC_SAMBA4X_EMBEDDED_SRVSVC
+	status = tc_smbd_open_embedded_srvsvc_np(
+		name,
+		remote_client_address,
+		local_server_address,
+		session_info,
+		ev_ctx,
+		msg_ctx,
+		global_dcesrv_context(), /* rc2 np_open no longer receives this */
+		npa);
+	if (NT_STATUS_IS_OK(status)) {
+		*phandle = handle;
+		return NT_STATUS_OK;
+	}
+	if (!NT_STATUS_EQUAL(status, NT_STATUS_OBJECT_NAME_NOT_FOUND)) {
+		DBG_DEBUG("Embedded srvsvc pipe open failed: %s\n",
+			  nt_errstr(status));
+		TALLOC_FREE(handle);
+		return status;
+	}
+#endif
+
 	ret = local_np_connect(
 		name,
 		NCACN_NP,
diff --git a/source3/wscript_build b/source3/wscript_build
--- a/source3/wscript_build
+++ b/source3/wscript_build
@@ -643,6 +643,24 @@ if bld.CONFIG_SET('WITH_SMB1SERVER'):
 else:
     SMB1_SOURCES = ''
 
+tc_smbd_embedded_srvsvc_sources = ''
+tc_smbd_embedded_srvsvc_deps = ''
+if bld.env.TC_SAMBA4X_EMBEDDED_SRVSVC:
+    # Time Capsule appliance builds deploy only smbd. Keep srvsvc explicit so
+    # future size audits can disable this endpoint without changing unrelated
+    # RPC code.
+    tc_smbd_embedded_srvsvc_sources = '''
+                          rpc_server/rpc_handles.c
+                          rpc_server/rpc_server.c
+                          '''
+    tc_smbd_embedded_srvsvc_deps = '''
+                        dcerpc-server-core
+                        RPC_CONFIG
+                        RPC_SRVSVC
+                        NDR_NAMED_PIPE_AUTH
+                        npa_tstream
+                        '''
+
 bld.SAMBA3_LIBRARY('smbd_base',
                    source='''
                           smbd/server_reload.c
@@ -735,7 +753,7 @@ bld.SAMBA3_LIBRARY('smbd_base',
                           smbd/conn.c
                           rpc_server/srv_pipe_hnd.c
                           rpc_server/rpc_ncacn_np.c
-                          ''' + NOTIFY_SOURCES + SMB1_SOURCES,
+                          ''' + tc_smbd_embedded_srvsvc_sources + NOTIFY_SOURCES + SMB1_SOURCES,
                    deps='''
                         talloc
                         tevent
@@ -767,6 +785,7 @@ bld.SAMBA3_LIBRARY('smbd_base',
                         fd_handle
                         cli_spoolss
                         samba3-namearray
+                        ''' + tc_smbd_embedded_srvsvc_deps + '''
                         LIBTLS
                    ''' +
                    bld.env['dmapi_lib'] +
