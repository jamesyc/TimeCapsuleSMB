diff --git a/dynconfig/wscript b/dynconfig/wscript
index 18ca145d3c8..851646a569a 100644
--- a/dynconfig/wscript
+++ b/dynconfig/wscript
@@ -389,10 +389,14 @@ def get_override(bld):
                  'PYTHONDIR'     : 'bin/python',
                  'PYTHONARCHDIR' : 'bin/python',
                  'BINDIR'        : 'bin',
                  'SBINDIR'       : 'bin',
-                 'LIBEXECDIR'    : 'bin',
-                 'SAMBA_LIBEXECDIR' : 'bin',
+                 # Time Capsule ships the build-tree smbd/rpc helper binaries
+                 # directly instead of running "waf install" on-device. The
+                 # upstream non-install override rewrites helper discovery to
+                 # the VM checkout's bin/ directory, which does not exist on
+                 # the appliance. Preserve the configured install libexec paths
+                 # so samba-dcerpcd scans /root/tc-netbsd*/libexec/samba.
                  'CODEPAGEDIR'   : 'codepages',
                  'SCRIPTSBINDIR' : 'source4/scripting/bin',
                  'SETUPDIR'      : 'source4/setup'
                  }
