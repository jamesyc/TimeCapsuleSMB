diff --git a/source3/modules/util_reparse.c b/source3/modules/util_reparse.c
--- a/source3/modules/util_reparse.c
+++ b/source3/modules/util_reparse.c
@@ -187,6 +187,28 @@ static NTSTATUS fsctl_get_reparse_point_lnk(struct files_struct *fsp,
 		return status;
 	}
 
+	if (!fsp->fsp_flags.posix_open &&
+	    tc_native_links_enabled(fsp->conn)) {
+		/*
+		 * Non-POSIX clients (macOS and Windows) read the target in
+		 * their own namespace: names mapped as catia maps them (a
+		 * native "a:b" is "a<U+F022>b"; macOS maps them back with
+		 * UTF_SFM_CONVERSIONS, Windows would take ':' for a stream),
+		 * and '\\' separators they convert back themselves. Map the
+		 * names first: catia maps a literal '\\' in a name to U+F026.
+		 */
+		status = tc_native_links_map_target(
+			fsp->conn,
+			reparse,
+			&reparse->parsed.lnk.substitute_name,
+			vfs_translate_to_windows);
+		if (!NT_STATUS_IS_OK(status)) {
+			TALLOC_FREE(reparse);
+			return status;
+		}
+		string_replace(reparse->parsed.lnk.substitute_name, '/', '\\');
+	}
+
 	status = fsctl_get_reparse_point_int(
 		fsp, reparse, mem_ctx, _out_data, max_out_len, _out_len);
 	TALLOC_FREE(reparse);
@@ -343,6 +365,11 @@ NTSTATUS fsctl_set_reparse_point(struct files_struct *fsp,
 		  reparse_tag,
 		  reparse_data_length);
 
+	status = tc_native_links_check_set(fsp, reparse_tag, in_data, in_len);
+	if (!NT_STATUS_IS_OK(status)) {
+		return status;
+	}
+
 	status = fsctl_get_reparse_tag(fsp, &existing_tag);
 	if (NT_STATUS_IS_OK(status) && (existing_tag != reparse_tag)) {
 		DBG_DEBUG("Can't overwrite tag %" PRIX32 " with tag %" PRIX32
diff --git a/source3/modules/vfs_catia.c b/source3/modules/vfs_catia.c
--- a/source3/modules/vfs_catia.c
+++ b/source3/modules/vfs_catia.c
@@ -744,6 +744,91 @@ out:
 	return ret;
 }
 
+/*
+ * Time Capsule native links (patch 0045): symlinks are objects clients see,
+ * so their names need the same mapping as every other at-call. Without it a
+ * link named with a mapped character (':' '*' '?' ...) is created correctly
+ * but its target can never be read back.
+ */
+static struct smb_filename *catia_map_at_name(vfs_handle_struct *handle,
+					      const struct smb_filename *in)
+{
+	struct smb_filename *out = NULL;
+	char *name = NULL;
+	int ret;
+
+	ret = catia_string_replace_allocate(handle->conn,
+					    in->base_name,
+					    &name,
+					    vfs_translate_to_unix);
+	if (ret != 0) {
+		errno = ret;
+		return NULL;
+	}
+	out = cp_smb_filename(talloc_tos(), in);
+	if (out == NULL) {
+		TALLOC_FREE(name);
+		errno = ENOMEM;
+		return NULL;
+	}
+	TALLOC_FREE(out->base_name);
+	out->base_name = talloc_move(out, &name);
+	out->fsp = in->fsp;
+	return out;
+}
+
+static int catia_symlinkat(vfs_handle_struct *handle,
+			   const struct smb_filename *link_contents,
+			   struct files_struct *dirfsp,
+			   const struct smb_filename *new_smb_fname)
+{
+	struct catia_cache *cc = NULL;
+	struct smb_filename *mapped = NULL;
+	int ret;
+
+	ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, dirfsp, &cc);
+	if (ret != 0) {
+		return ret;
+	}
+	mapped = catia_map_at_name(handle, new_smb_fname);
+	if (mapped == NULL) {
+		ret = -1;
+		goto out;
+	}
+	ret = SMB_VFS_NEXT_SYMLINKAT(handle, link_contents, dirfsp, mapped);
+	TALLOC_FREE(mapped);
+out:
+	CATIA_FETCH_FSP_POST_NEXT(&cc, dirfsp);
+	return ret;
+}
+
+static int catia_readlinkat(vfs_handle_struct *handle,
+			    const struct files_struct *dirfsp,
+			    const struct smb_filename *smb_fname,
+			    char *buf,
+			    size_t bufsiz)
+{
+	struct files_struct *dir = discard_const_p(struct files_struct, dirfsp);
+	struct catia_cache *cc = NULL;
+	struct smb_filename *mapped = NULL;
+	int ret;
+
+	ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, dir, &cc);
+	if (ret != 0) {
+		return ret;
+	}
+	mapped = catia_map_at_name(handle, smb_fname);
+	if (mapped == NULL) {
+		ret = -1;
+		goto out;
+	}
+	ret = SMB_VFS_NEXT_READLINKAT(handle, dirfsp, mapped, buf, bufsiz);
+	TALLOC_FREE(mapped);
+out:
+	CATIA_FETCH_FSP_POST_NEXT(&cc, dir);
+	return ret;
+}
+
 static int catia_lchown(vfs_handle_struct *handle,
 			const struct smb_filename *smb_fname,
 			uid_t uid,
@@ -962,6 +1047,33 @@ catia_fstreaminfo(struct vfs_handle_struct *handle,
 	*_num_streams = 0;
 	*_streams = NULL;
 
+	if (fsp_get_pathref_fd(fsp) == -1 &&
+	    S_ISLNK(smb_fname->st.st_ex_mode) &&
+	    tc_native_links_enabled(handle->conn))
+	{
+		/*
+		 * A Time Capsule native link opened as itself: re-resolving
+		 * it by name below would follow it and list its target's
+		 * streams. Translate this handle's name instead.
+		 */
+		struct catia_cache *cc = NULL;
+
+		ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, fsp, &cc);
+		if (ret != 0) {
+			return map_nt_error_from_unix(errno);
+		}
+		status = SMB_VFS_NEXT_FSTREAMINFO(handle,
+						  fsp,
+						  mem_ctx,
+						  &num_streams,
+						  &streams);
+		CATIA_FETCH_FSP_POST_NEXT(&cc, fsp);
+		if (!NT_STATUS_IS_OK(status)) {
+			return status;
+		}
+		goto translate;
+	}
+
 	ret = catia_string_replace_allocate(handle->conn,
 					    smb_fname->base_name,
 					    &mapped_name,
@@ -995,6 +1107,8 @@ catia_fstreaminfo(struct vfs_handle_struct *handle,
 		return status;
 	}
 
+translate:
+
 	/*
 	 * Translate stream names just like the base names
 	 */
@@ -1149,6 +1263,7 @@ static ssize_t catia_fgetxattr(struct vfs_handle_struct *handle,
 			       void *value,
 			       size_t size)
 {
+	struct catia_cache *cc = NULL;
 	char *mapped_xattr_name = NULL;
 	ssize_t result;
 	int ret;
@@ -1162,9 +1277,20 @@ static ssize_t catia_fgetxattr(struct vfs_handle_struct *handle,
 		return -1;
 	}
 
+	/*
+	 * A native link opened as itself has no fd; the backend reaches it
+	 * by path, so it needs the mapped name as well (patch 0045).
+	 */
+	ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, fsp, &cc);
+	if (ret != 0) {
+		TALLOC_FREE(mapped_xattr_name);
+		return -1;
+	}
+
 	result = SMB_VFS_NEXT_FGETXATTR(handle, fsp, mapped_xattr_name,
 					value, size);
 
+	CATIA_FETCH_FSP_POST_NEXT(&cc, fsp);
 	TALLOC_FREE(mapped_xattr_name);
 
 	return result;
@@ -1195,6 +1321,7 @@ static int catia_fremovexattr(struct vfs_handle_struct *handle,
 			      struct files_struct *fsp,
 			      const char *name)
 {
+	struct catia_cache *cc = NULL;
 	char *mapped_name = NULL;
 	int ret;
 
@@ -1207,8 +1334,15 @@ static int catia_fremovexattr(struct vfs_handle_struct *handle,
 		return -1;
 	}
 
+	ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, fsp, &cc);
+	if (ret != 0) {
+		TALLOC_FREE(mapped_name);
+		return -1;
+	}
+
 	ret = SMB_VFS_NEXT_FREMOVEXATTR(handle, fsp, mapped_name);
 
+	CATIA_FETCH_FSP_POST_NEXT(&cc, fsp);
 	TALLOC_FREE(mapped_name);
 
 	return ret;
@@ -1221,6 +1355,7 @@ static int catia_fsetxattr(struct vfs_handle_struct *handle,
 			   size_t size,
 			   int flags)
 {
+	struct catia_cache *cc = NULL;
 	char *mapped_xattr_name = NULL;
 	int ret;
 
@@ -1233,9 +1368,16 @@ static int catia_fsetxattr(struct vfs_handle_struct *handle,
 		return -1;
 	}
 
+	ret = CATIA_FETCH_FSP_PRE_NEXT(talloc_tos(), handle, fsp, &cc);
+	if (ret != 0) {
+		TALLOC_FREE(mapped_xattr_name);
+		return -1;
+	}
+
 	ret = SMB_VFS_NEXT_FSETXATTR(handle, fsp, mapped_xattr_name,
 				     value, size, flags);
 
+	CATIA_FETCH_FSP_POST_NEXT(&cc, fsp);
 	TALLOC_FREE(mapped_xattr_name);
 
 	return ret;
@@ -1964,6 +2106,8 @@ static struct vfs_fn_pointers vfs_catia_fns = {
 	.fstatat_fn = catia_fstatat,
 	.lstat_fn = catia_lstat,
 	.unlinkat_fn = catia_unlinkat,
+	.symlinkat_fn = catia_symlinkat,
+	.readlinkat_fn = catia_readlinkat,
 	.fchmod_fn = catia_fchmod,
 	.fchown_fn = catia_fchown,
 	.lchown_fn = catia_lchown,
diff --git a/source3/modules/vfs_default.c b/source3/modules/vfs_default.c
--- a/source3/modules/vfs_default.c
+++ b/source3/modules/vfs_default.c
@@ -2965,8 +2965,19 @@ static int vfswrap_fchmod(vfs_handle_struct *handle, files_struct *fsp, mode_t m
 	}
 
 	/*
-	 * This is no longer a handle based call.
+	 * This is no longer a handle based call. A symlink pathref must not
+	 * follow the link (Time Capsule native links, patch 0045).
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+#ifdef HAVE_LCHMOD
+		result = lchmod(fsp->fsp_name->base_name, mode);
+#else
+		errno = ENOTSUP;
+		result = -1;
+#endif
+		END_PROFILE_X(syscall_fchmod);
+		return result;
+	}
 	result = chmod(fsp->fsp_name->base_name, mode);
 
 	END_PROFILE_X(syscall_fchmod);
@@ -2996,8 +3007,13 @@ static int vfswrap_fchown(vfs_handle_struct *handle, files_struct *fsp, uid_t ui
 	}
 
 	/*
-	 * This is no longer a handle based call.
+	 * This is no longer a handle based call. Never follow a symlink.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		result = lchown(fsp->fsp_name->base_name, uid, gid);
+		END_PROFILE_X(syscall_fchown);
+		return result;
+	}
 	result = chown(fsp->fsp_name->base_name, uid, gid);
 	END_PROFILE_X(syscall_fchown);
 	return result;
@@ -3081,6 +3097,20 @@ static int vfswrap_fntimes(vfs_handle_struct *handle,
 		times = NULL;
 	}
 
+	if (fsp_get_pathref_fd(fsp) == -1 &&
+	    S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		/*
+		 * A symlink opened as itself (Time Capsule native links) has
+		 * no fd. Set the link's times; following it would change the
+		 * target's, or fail for a dangling link.
+		 */
+		result = utimensat(AT_FDCWD,
+				   fsp->fsp_name->base_name,
+				   times,
+				   AT_SYMLINK_NOFOLLOW);
+		goto out;
+	}
+
 	if (!fsp->fsp_flags.is_pathref) {
 		result = futimens(fsp_get_io_fd(fsp), times);
 		goto out;
@@ -3547,8 +3577,18 @@ static int vfswrap_fchflags(vfs_handle_struct *handle,
 	}
 
 	/*
-	 * This is no longer a handle based call.
+	 * This is no longer a handle based call. Never follow a symlink.
+	 * vfs_catia has no fchflags hook, so a link name with a mapped
+	 * character fails here with ENOENT; SMB never sets flags on links.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+#ifdef HAVE_LCHFLAGS
+		return lchflags(fsp->fsp_name->base_name, flags);
+#else
+		errno = ENOTSUP;
+		return -1;
+#endif
+	}
 	return chflags(fsp->fsp_name->base_name, flags);
 #else
 	errno = ENOSYS;
@@ -3793,8 +3833,14 @@ static ssize_t vfswrap_fgetxattr(struct vfs_handle_struct *handle,
 	}
 
 	/*
-	 * This is no longer a handle based call.
+	 * This is no longer a handle based call. A symlink's own attributes
+	 * are served by the native HFS backend; never read the target's.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		errno = ENOATTR;
+		res = -1;
+		goto out;
+	}
 	res = getxattr(fsp->fsp_name->base_name, name, value, size);
 out:
 	END_PROFILE_X(syscall_fgetxattr);
@@ -4133,6 +4179,11 @@ static ssize_t vfswrap_flistxattr(struct vfs_handle_struct *handle, struct files
 	/*
 	 * This is no longer a handle based call.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		/* Never list a symlink target's attributes. */
+		res = 0;
+		goto out;
+	}
 	res = listxattr(fsp->fsp_name->base_name, list, size);
 out:
 	END_PROFILE_X(syscall_flistxattr);
@@ -4163,6 +4214,12 @@ static int vfswrap_fremovexattr(struct vfs_handle_struct *handle, struct files_s
 	/*
 	 * This is no longer a handle based call.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		/* Never change a symlink target's attributes. */
+		errno = ENOTSUP;
+		res = -1;
+		goto out;
+	}
 	res = removexattr(fsp->fsp_name->base_name, name);
 out:
 	END_PROFILE_X(syscall_fremovexattr);
@@ -4197,6 +4254,12 @@ static int vfswrap_fsetxattr(struct vfs_handle_struct *handle, struct files_stru
 	/*
 	 * This is no longer a handle based call.
 	 */
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
+		/* Never change a symlink target's attributes. */
+		errno = ENOTSUP;
+		res = -1;
+		goto out;
+	}
 	res = setxattr(fsp->fsp_name->base_name, name, value, size, flags);
 out:
 	END_PROFILE_X(syscall_fsetxattr);
diff --git a/source3/modules/vfs_fruit.c b/source3/modules/vfs_fruit.c
--- a/source3/modules/vfs_fruit.c
+++ b/source3/modules/vfs_fruit.c
@@ -1126,6 +1126,8 @@ static NTSTATUS check_aapl(vfs_handle_struct *handle,
 				      blob);
 	if (NT_STATUS_IS_OK(status)) {
 		global_fruit_config.nego_aapl = true;
+		/* macOS resolves symlinks client side; see tc_native_links.c */
+		handle->conn->sconn->client_resolves_symlinks = true;
 	}
 
 	return status;
diff --git a/source3/modules/vfs_streams_xattr.c b/source3/modules/vfs_streams_xattr.c
--- a/source3/modules/vfs_streams_xattr.c
+++ b/source3/modules/vfs_streams_xattr.c
@@ -676,9 +676,19 @@ static int streams_xattr_fstat(vfs_handle_struct *handle, files_struct *fsp,
 		return -1;
 	}
 
-	ret = SMB_VFS_NEXT_FSTAT(handle, fsp->base_fsp, sbuf);
-	if (ret == -1) {
-		return -1;
+	if (fsp_get_pathref_fd(fsp->base_fsp) == -1 &&
+	    S_ISLNK(fsp->base_fsp->fsp_name->st.st_ex_mode) &&
+	    tc_native_links_enabled(fsp->conn)) {
+		/*
+		 * A stream on a Time Capsule native link: the fd-less base
+		 * already carries the link's own lstat (see vfs_stat_fsp()).
+		 */
+		*sbuf = fsp->base_fsp->fsp_name->st;
+	} else {
+		ret = SMB_VFS_NEXT_FSTAT(handle, fsp->base_fsp, sbuf);
+		if (ret == -1) {
+			return -1;
+		}
 	}
 
 	sbuf->st_ex_size = get_xattr_size_fsp(config,
diff --git a/source3/modules/vfs_xattr_tdb.c b/source3/modules/vfs_xattr_tdb.c
--- a/source3/modules/vfs_xattr_tdb.c
+++ b/source3/modules/vfs_xattr_tdb.c
@@ -77,6 +77,135 @@ static bool tc_fruit_owned_native_xattr(const char *name)
 		strcmp(name, TC_RESOURCEFORK_XATTR) == 0;
 }
 
+/*
+ * A native symlink opened as itself (Time Capsule native links, patch 0045)
+ * has no descriptor. Its attributes live on the link, as Apple's AFP server
+ * stores them, and are reached by path with the no-follow syscalls. Any
+ * other descriptor-less handle keeps failing with EBADF.
+ *
+ * The handle keeps only the link's name, and an AFP or SSH client can rename
+ * the link between two SMB requests and put another object there. So check
+ * that the name still holds the link this handle opened, by the file id taken
+ * at open (vfs_stat_fsp() re-lstats the name and would follow a replacement),
+ * and fail with ENOENT otherwise. This is the practical fix, not an atomic
+ * one: NetBSD cannot open a symlink itself (no O_PATH or O_SYMLINK), so
+ * nothing binds the syscall to the inode, and the name can still change in
+ * the microseconds between the lstat() and the syscall. vfs_default's own
+ * path-based pathref calls ("This is no longer a handle based call") have
+ * the same gap with no check at all; it is tolerable.
+ *
+ * Returns 0 with *_path NULL for a handle that has a descriptor.
+ */
+static int tc_native_link_path(files_struct *fsp, char **_path)
+{
+	struct stat st;
+	char *path = NULL;
+
+	*_path = NULL;
+	if (fsp_get_pathref_fd(fsp) != -1 || fsp->fsp_name == NULL ||
+	    !S_ISLNK(fsp->fsp_name->st.st_ex_mode))
+	{
+		return 0;
+	}
+	path = talloc_asprintf(talloc_tos(), "%s/%s",
+			       fsp->conn->connectpath,
+			       fsp->fsp_name->base_name);
+	if (path == NULL) {
+		errno = ENOMEM;
+		return -1;
+	}
+	if (lstat(path, &st) != 0 || !S_ISLNK(st.st_mode) ||
+	    (uint64_t)st.st_dev != fsp->file_id.devid ||
+	    (uint64_t)st.st_ino != fsp->file_id.inode)
+	{
+		DBG_NOTICE("%s is no longer the link this handle opened\n",
+			   path);
+		TALLOC_FREE(path);
+		errno = ENOENT;
+		return -1;
+	}
+	*_path = path;
+	return 0;
+}
+
+static ssize_t tc_native_fgetxattr(files_struct *fsp, const char *name,
+				   void *value, size_t size)
+{
+	char *path = NULL;
+	ssize_t ret;
+	int err;
+
+	if (tc_native_link_path(fsp, &path) != 0) {
+		return -1;
+	}
+	if (path == NULL) {
+		return tc_airport_fgetxattr(
+			fsp_get_pathref_fd(fsp), name, value, size);
+	}
+	ret = tc_airport_lgetxattr(path, name, value, size);
+	err = errno;
+	TALLOC_FREE(path);
+	errno = err;
+	return ret;
+}
+
+static int tc_native_fsetxattr(files_struct *fsp, const char *name,
+			       const void *value, size_t size, int flags)
+{
+	char *path = NULL;
+	int ret, err;
+
+	if (tc_native_link_path(fsp, &path) != 0) {
+		return -1;
+	}
+	if (path == NULL) {
+		return tc_airport_fsetxattr(
+			fsp_get_pathref_fd(fsp), name, value, size, flags);
+	}
+	ret = tc_airport_lsetxattr(path, name, value, size, flags);
+	err = errno;
+	TALLOC_FREE(path);
+	errno = err;
+	return ret;
+}
+
+static ssize_t tc_native_flistxattr(files_struct *fsp, char *list, size_t size)
+{
+	char *path = NULL;
+	ssize_t ret;
+	int err;
+
+	if (tc_native_link_path(fsp, &path) != 0) {
+		return -1;
+	}
+	if (path == NULL) {
+		return tc_airport_flistxattr(fsp_get_pathref_fd(fsp), list, size);
+	}
+	ret = tc_airport_llistxattr(path, list, size);
+	err = errno;
+	TALLOC_FREE(path);
+	errno = err;
+	return ret;
+}
+
+static int tc_native_fremovexattr(files_struct *fsp, const char *name)
+{
+	char *path = NULL;
+	int ret, err;
+
+	if (tc_native_link_path(fsp, &path) != 0) {
+		return -1;
+	}
+	if (path == NULL) {
+		return tc_airport_fremovexattr(fsp_get_pathref_fd(fsp), name);
+	}
+	ret = tc_airport_lremovexattr(path, name);
+	err = errno;
+	TALLOC_FREE(path);
+	errno = err;
+	return ret;
+}
+
 static ssize_t tc_native_apple_stream_get(files_struct *fsp,
 					   const char *native_name,
 					   void *value,
@@ -84,9 +213,8 @@ static ssize_t tc_native_apple_stream_get(files_struct *fsp,
 {
 	ssize_t raw_size;
 	ssize_t ret;
-	int fd = fsp_get_pathref_fd(fsp);
 
-	raw_size = tc_airport_fgetxattr(fd, native_name, NULL, 0);
+	raw_size = tc_native_fgetxattr(fsp, native_name, NULL, 0);
 	if (raw_size < 0) {
 		return -1;
 	}
@@ -101,7 +229,7 @@ static ssize_t tc_native_apple_stream_get(files_struct *fsp,
 		errno = ERANGE;
 		return -1;
 	}
-	ret = tc_airport_fgetxattr(fd, native_name, value, raw_size);
+	ret = tc_native_fgetxattr(fsp, native_name, value, raw_size);
 	if (ret < 0) {
 		return -1;
 	}
@@ -129,8 +257,7 @@ static int tc_native_apple_stream_set(files_struct *fsp,
 		errno = E2BIG;
 		return -1;
 	}
-	return tc_airport_fsetxattr(
-		fsp_get_pathref_fd(fsp), native_name, value, size - 1, flags);
+	return tc_native_fsetxattr(fsp, native_name, value, size - 1, flags);
 }
 
 static ssize_t tc_native_list_with_apple_streams(
@@ -143,7 +270,7 @@ static ssize_t tc_native_list_with_apple_streams(
 	size_t required = 0;
 	size_t written_size = 0;
 
-	raw_size = tc_airport_flistxattr(fsp_get_pathref_fd(fsp), NULL, 0);
+	raw_size = tc_native_flistxattr(fsp, NULL, 0);
 	if (raw_size <= 0) {
 		return raw_size;
 	}
@@ -152,8 +279,7 @@ static ssize_t tc_native_list_with_apple_streams(
 		errno = ENOMEM;
 		return -1;
 	}
-	ret = tc_airport_flistxattr(
-		fsp_get_pathref_fd(fsp), raw, raw_size);
+	ret = tc_native_flistxattr(fsp, raw, raw_size);
 	if (ret < 0) {
 		TALLOC_FREE(raw);
 		return -1;
@@ -306,8 +432,7 @@ static struct tevent_req *xattr_tdb_getxattrat_send(
 			xattr_size = tc_native_apple_stream_get(
 				fsp, native_name, NULL, 0);
 		} else {
-			xattr_size = tc_airport_fgetxattr(
-				fsp_get_pathref_fd(fsp), xattr_name, NULL, 0);
+			xattr_size = tc_native_fgetxattr(fsp, xattr_name, NULL, 0);
 		}
 		if (xattr_size < 0) {
 			tevent_req_error(req, errno);
@@ -342,9 +467,7 @@ static struct tevent_req *xattr_tdb_getxattrat_send(
 				state->xattr_value,
 				state->xattr_size);
 		} else {
-			xattr_size = tc_airport_fgetxattr(
-				fsp_get_pathref_fd(fsp),
-				xattr_name,
+			xattr_size = tc_native_fgetxattr(fsp, xattr_name,
 				state->xattr_value,
 				state->xattr_size);
 		}
@@ -486,8 +609,7 @@ static ssize_t xattr_tdb_fgetxattr(struct vfs_handle_struct *handle,
 			errno = ENOTSUP;
 			return -1;
 		}
-		return tc_airport_fgetxattr(
-			fsp_get_pathref_fd(fsp), name, value, size);
+		return tc_native_fgetxattr(fsp, name, value, size);
 	}
 
 	if (config->ignore_user_xattr && is_user_xattr(name)) {
@@ -557,8 +679,7 @@ static int xattr_tdb_fsetxattr(struct vfs_handle_struct *handle,
 			errno = ENOTSUP;
 			return -1;
 		}
-		return tc_airport_fsetxattr(
-			fsp_get_pathref_fd(fsp), name, value, size, flags);
+		return tc_native_fsetxattr(fsp, name, value, size, flags);
 	}
 
 	if (config->ignore_user_xattr && is_user_xattr(name)) {
@@ -645,8 +766,7 @@ static int xattr_tdb_fremovexattr(struct vfs_handle_struct *handle,
 				errno = ENOATTR;
 				return -1;
 			}
-			ret = tc_airport_fremovexattr(
-				fsp_get_pathref_fd(fsp), native_name);
+			ret = tc_native_fremovexattr(fsp, native_name);
 			TALLOC_FREE(native_name);
 			return ret;
 		}
@@ -654,7 +774,7 @@ static int xattr_tdb_fremovexattr(struct vfs_handle_struct *handle,
 			errno = ENOTSUP;
 			return -1;
 		}
-		return tc_airport_fremovexattr(fsp_get_pathref_fd(fsp), name);
+		return tc_native_fremovexattr(fsp, name);
 	}
 
 	if (config->ignore_user_xattr && is_user_xattr(name)) {
diff --git a/source3/smbd/close.c b/source3/smbd/close.c
--- a/source3/smbd/close.c
+++ b/source3/smbd/close.c
@@ -161,6 +161,17 @@ NTSTATUS delete_all_streams(struct files_struct *fsp,
 	TALLOC_CTX *frame = talloc_stackframe();
 	NTSTATUS status;
 
+	if (S_ISLNK(smb_fname->st.st_ex_mode) &&
+	    tc_native_links_enabled(fsp->conn)) {
+		/*
+		 * A Time Capsule native link's streams are attributes of the
+		 * link itself, removed with it by unlink. Deleting them by
+		 * name here would resolve through the link to its target.
+		 */
+		TALLOC_FREE(frame);
+		return NT_STATUS_OK;
+	}
+
 	status = vfs_fstreaminfo(fsp,
 				 talloc_tos(),
 				 &num_streams,
@@ -344,6 +355,8 @@ struct close_share_mode_lock_state {
 	const struct security_token *del_nt_token;
 	bool reset_delete_on_close;
 	share_mode_entry_prepare_unlock_fn_t cleanup_fn;
+	struct tc_xsym_candidate *tc_xsym;
+	bool tc_convert;
 };
 
 static void close_share_mode_lock_prepare(struct share_mode_lock *lck,
@@ -413,6 +426,19 @@ static void close_share_mode_lock_prepare(struct share_mode_lock *lck,
 		state->delete_object = false;
 	}
 
+	if (!state->delete_object && state->tc_xsym != NULL &&
+	    tc_native_links_sole_open(lck, fsp)) {
+		/*
+		 * This handle created an XSym file or symlink placeholder and
+		 * is its only open: replace it with a native link while the
+		 * lock is held, as delete on close does. The entry goes on
+		 * unlock (close_share_mode_lock_cleanup).
+		 */
+		state->tc_convert = true;
+		*keep_locked = true;
+		return;
+	}
+
 	if (!state->delete_object) {
 		ok = del_share_mode(lck, fsp);
 		if (!ok) {
@@ -464,7 +490,8 @@ static void close_share_mode_lock_cleanup(struct share_mode_lock *lck,
 ****************************************************************************/
 
 static NTSTATUS close_remove_share_mode(files_struct *fsp,
-					enum file_close_type close_type)
+					enum file_close_type close_type,
+					struct tc_xsym_candidate *tc_xsym)
 {
 	connection_struct *conn = fsp->conn;
 	struct close_share_mode_lock_state lck_state = {};
@@ -487,6 +514,7 @@ static NTSTATUS close_remove_share_mode(files_struct *fsp,
 		.fsp			= fsp,
 		.object_type		= "file",
 		.close_type		= close_type,
+		.tc_xsym		= tc_xsym,
 	};
 
 	status = share_mode_entry_prepare_lock_del(&lck_state.prepare_state,
@@ -515,6 +543,13 @@ static NTSTATUS close_remove_share_mode(files_struct *fsp,
 	 * reference to a file.
 	 */
 
+	if (lck_state.tc_convert) {
+		lck_state.cleanup_fn = close_share_mode_lock_cleanup;
+		tc_native_links_close_commit(fsp, tc_xsym);
+		status = NT_STATUS_OK;
+		goto done;
+	}
+
 	if (!lck_state.delete_object) {
 		status = NT_STATUS_OK;
 		goto done;
@@ -909,6 +944,8 @@ static NTSTATUS close_normal_file(struct smb_request *req, files_struct *fsp,
 	NTSTATUS status = NT_STATUS_OK;
 	NTSTATUS tmp;
 	connection_struct *conn = fsp->conn;
+	struct tc_xsym_candidate tc_xsym;
+	bool tc_convert;
 	bool is_durable;
 
 	SMB_ASSERT(fsp->fsp_flags.is_fsa);
@@ -942,12 +979,26 @@ static NTSTATUS close_normal_file(struct smb_request *req, files_struct *fsp,
 			     fsp_get_smb2_lease(fsp));
 	}
 
+	/*
+	 * A client that just wrote a Minshall+French XSym file (macOS without
+	 * reparse support, Linux mfsymlinks) or a symlink reparse placeholder
+	 * asked for a symlink. close_remove_share_mode() replaces it with a
+	 * native link if this is its only open.
+	 */
+	tc_convert = tc_native_links_close_prepare(talloc_tos(),
+						   fsp,
+						   close_type,
+						   &tc_xsym);
+
 	/* If this is an old DOS or FCB open and we have multiple opens on
 	   the same handle we only have one share mode. Ensure we only remove
 	   the share mode on the last close. */
 
-	tmp = close_remove_share_mode(fsp, close_type);
+	tmp = close_remove_share_mode(fsp,
+				      close_type,
+				      tc_convert ? &tc_xsym : NULL);
 	status = ntstatus_keeperror(status, tmp);
+	TALLOC_FREE(tc_xsym.target);
 
 	/*
 	 * Ensure pending modtime is set before closing underlying fd.
@@ -966,6 +1017,9 @@ static NTSTATUS close_normal_file(struct smb_request *req, files_struct *fsp,
 	tmp = fd_close(fsp);
 	status = ntstatus_keeperror(status, tmp);
 
+	/* Remove a file a native-link conversion took aside, now closed. */
+	tc_native_links_close_finish(fsp, &tc_xsym);
+
 	/* check for magic scripts */
 	if (close_type == NORMAL_CLOSE) {
 		tmp = check_magic(fsp);
diff --git a/source3/smbd/dir.c b/source3/smbd/dir.c
--- a/source3/smbd/dir.c
+++ b/source3/smbd/dir.c
@@ -646,9 +646,12 @@ bool smbd_dirptr_get_entry(TALLOC_CTX *ctx,
 			goto done;
 		}
 
-		if (posix) {
+		if (posix || tc_native_links_enabled(conn)) {
 			/*
-			 * Posix always wants to see symlinks.
+			 * Posix always wants to see symlinks. With Time
+			 * Capsule native links every client sees them too,
+			 * as symlink reparse points, including dangling ones,
+			 * so they can be listed and removed like on AFP.
 			 */
 			goto done;
 		}
diff --git a/source3/smbd/dosmode.c b/source3/smbd/dosmode.c
--- a/source3/smbd/dosmode.c
+++ b/source3/smbd/dosmode.c
@@ -762,6 +762,8 @@ uint32_t fdos_mode(struct files_struct *fsp)
 					     fsp);
 	}
 
+	result = tc_native_links_dos_mode(fsp, result);
+
 	fsp->fsp_name->st.cached_dos_attributes = dos_mode_post(result, fsp, __func__);
 	return fsp->fsp_name->st.cached_dos_attributes;
 }
diff --git a/source3/smbd/filename.c b/source3/smbd/filename.c
--- a/source3/smbd/filename.c
+++ b/source3/smbd/filename.c
@@ -43,6 +43,13 @@ uint32_t ucf_flags_from_smb_request(struct smb_request *req)
 	if (req->posix_pathnames) {
 		ucf_flags |= (UCF_POSIX_PATHNAMES|UCF_LCOMP_LNK_OK);
 	}
+	if (tc_client_resolves_links(req->conn)) {
+		/*
+		 * A terminal symlink is the object for macOS, even when a
+		 * rename open omits FILE_OPEN_REPARSE_POINT.
+		 */
+		ucf_flags |= UCF_LCOMP_LNK_OK;
+	}
 	if (req->flags2 & FLAGS2_DFS_PATHNAMES) {
 		ucf_flags |= UCF_DFS_PATHNAME;
 	}
@@ -1156,6 +1163,24 @@ next:
 	 * resolve all symlinks locally.
 	 */
 
+	if (tc_native_links_enabled(conn)) {
+		/*
+		 * Time Capsule native links (patch 0045): the target is joined
+		 * with the client's path and resolved again as a client name,
+		 * so map its native names to theirs first. Unmapped, a ':' in
+		 * the target would split off a stream name, and catia maps
+		 * the names back to the disk's on the way down.
+		 */
+		status = tc_native_links_map_target(conn,
+						    symlink_err,
+						    &lnk->substitute_name,
+						    vfs_translate_to_windows);
+		if (!NT_STATUS_IS_OK(status)) {
+			TALLOC_FREE(symlink_err);
+			return status;
+		}
+	}
+
 	ret = symlink_target_path(mem_ctx,
 				  name_in,
 				  lnk->unparsed_path_length,
diff --git a/source3/smbd/files.c b/source3/smbd/files.c
--- a/source3/smbd/files.c
+++ b/source3/smbd/files.c
@@ -882,7 +882,13 @@ NTSTATUS read_symlink_reparse(TALLOC_CTX *mem_ctx,
 		goto fail;
 	}
 
-	if (lnk->substitute_name[0] == '/') {
+	/*
+	 * Time Capsule native links keep absolute targets verbatim, as
+	 * Apple's AFP server shows them. Following them on the server still
+	 * works: safe_symlink_target_path() resolves absolute in-share paths.
+	 */
+	if (lnk->substitute_name[0] == '/' &&
+	    !tc_native_links_enabled(dirfsp->conn)) {
 		size_t len = fsp_fullbasepath(dirfsp, NULL, 0);
 		char subdir_path[len + 1];
 		char *abs_target_canon = NULL;
diff --git a/source3/smbd/globals.h b/source3/smbd/globals.h
--- a/source3/smbd/globals.h
+++ b/source3/smbd/globals.h
@@ -823,6 +823,13 @@ struct smbd_server_connection {
 
 	struct notify_mid_map *notify_mid_maps;
 
+	/*
+	 * Set by vfs_fruit once the client negotiates AAPL. macOS resolves
+	 * symlinks itself, so the Time Capsule native-link support never
+	 * follows a terminal link on its behalf (see tc_native_links.c).
+	 */
+	bool client_resolves_symlinks;
+
 	struct pthreadpool_tevent *pool;
 
 	struct smbXsrv_client *client;
diff --git a/source3/smbd/open.c b/source3/smbd/open.c
--- a/source3/smbd/open.c
+++ b/source3/smbd/open.c
@@ -1158,6 +1158,19 @@ static NTSTATUS open_file(
 		open_fd = true;
 	}
 
+	if (open_fd && !creating && !(how.flags & O_TRUNC) && file_existed &&
+	    S_ISLNK(smb_fname->st.st_ex_mode) &&
+	    tc_native_links_enabled(conn)) {
+		/*
+		 * A native link reached as the object itself (macOS, or
+		 * FILE_OPEN_REPARSE_POINT) has no data to read and cannot be
+		 * opened with O_NOFOLLOW. macOS asks for FILE_READ_DATA when it
+		 * reads the reparse point; give it the same fd-less handle a
+		 * stat open gets. can_read stays false below.
+		 */
+		open_fd = false;
+	}
+
 	if (open_fd) {
 		int ret;
 
@@ -5695,6 +5708,15 @@ static NTSTATUS open_streams_for_delete(connection_struct *conn,
 	} else {
 		pathref = smb_fname;
 	}
+	if (S_ISLNK(pathref->st.st_ex_mode) && tc_native_links_enabled(conn)) {
+		/*
+		 * Streams on a Time Capsule native link are the link's own
+		 * attributes. Opening them here by name would follow the link
+		 * to its target; they go with the link on rename or unlink.
+		 */
+		TALLOC_FREE(frame);
+		return NT_STATUS_OK;
+	}
 	status = vfs_fstreaminfo(pathref->fsp, talloc_tos(),
 				&num_streams, &stream_info);
 
@@ -6404,6 +6426,14 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 		case S_IFDIR:
 			break;
 		case S_IFLNK:
+			if (tc_client_resolves_links(conn)) {
+				/*
+				 * macOS opens its links as such without
+				 * FILE_OPEN_REPARSE_POINT (see
+				 * tc_native_links.c).
+				 */
+				break;
+			}
 			/*
 			 * We should never get this far with a symlink
 			 * "as such". Report as not existing.
@@ -6507,6 +6537,7 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 	    && is_named_stream(smb_fname))
 	{
 		uint32_t base_create_disposition;
+		uint32_t base_create_options;
 		struct smb_filename *smb_fname_base = NULL;
 		uint32_t base_privflags;
 
@@ -6540,7 +6571,20 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 		 * if the basefile exists we want a handle so we can fstat() it.
 		 */
 
-		status = openat_pathref_fsp(conn->cwd_fsp, smb_fname_base);
+		base_create_options = 0;
+		status = tc_native_links_stream_base(conn,
+						     dirfsp,
+						     smb_fname_base);
+		if (NT_STATUS_IS_OK(status)) {
+			/*
+			 * The base is a native link: its streams are the
+			 * link's own attributes, never its target's.
+			 */
+			base_create_options = FILE_OPEN_REPARSE_POINT;
+		} else if (NT_STATUS_EQUAL(status, NT_STATUS_NOT_A_REPARSE_POINT)) {
+			status = openat_pathref_fsp(conn->cwd_fsp,
+						    smb_fname_base);
+		}
 
 		if (!NT_STATUS_IS_OK(status) &&
 		    !NT_STATUS_EQUAL(status, NT_STATUS_OBJECT_NAME_NOT_FOUND))
@@ -6552,8 +6596,12 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 			goto fail;
 		}
 
-		if (NT_STATUS_IS_OK(status)) {
+		if (NT_STATUS_IS_OK(status) && base_create_options == 0) {
 			/*
+			 * A native link's mode bits are always rwxrwxrwx and
+			 * its fd-less pathref has no access mask to check;
+			 * links skip this base check (see above).
+			 *
 			 * https://bugzilla.samba.org/show_bug.cgi?id=10229
 			 * We need to check if the requested access mask
 			 * could be used to open the underlying file (if
@@ -6585,7 +6633,7 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 					      | FILE_SHARE_WRITE
 					      | FILE_SHARE_DELETE,
 					      base_create_disposition,
-					      0,
+					      base_create_options,
 					      0,
 					      0,
 					      NULL,
@@ -6712,6 +6760,19 @@ static NTSTATUS create_file_unixpath(connection_struct *conn,
 		}
 	}
 
+	if ((create_options & FILE_DIRECTORY_FILE) &&
+	    VALID_STAT(smb_fname->st) &&
+	    S_ISLNK(smb_fname->st.st_ex_mode) &&
+	    tc_native_links_enabled(conn)) {
+		/*
+		 * A native link opened as itself (FILE_OPEN_REPARSE_POINT) is
+		 * never a directory, even when Windows sees a directory
+		 * target's FILE_ATTRIBUTE_DIRECTORY on it: RemoveDirectoryW on
+		 * a directory symlink removes the link, never the target.
+		 */
+		create_options &= ~FILE_DIRECTORY_FILE;
+	}
+
 	/*
 	 * If it's a request for a directory open, deal with it separately.
 	 */
diff --git a/source3/smbd/proto.h b/source3/smbd/proto.h
--- a/source3/smbd/proto.h
+++ b/source3/smbd/proto.h
@@ -1233,4 +1233,6 @@ NTSTATUS delay_for_handle_lease_break_recv(struct tevent_req *req,
 					   TALLOC_CTX *mem_ctx,
 					   struct share_mode_lock **lck);
 
+#include "smbd/tc_native_links.h"
+
 #endif /* _SMBD_PROTO_H_ */
diff --git a/source3/smbd/smb2_create.c b/source3/smbd/smb2_create.c
--- a/source3/smbd/smb2_create.c
+++ b/source3/smbd/smb2_create.c
@@ -1360,7 +1360,9 @@ static struct tevent_req *smbd_smb2_create_send(TALLOC_CTX *mem_ctx,
 
 		mode_t mode = state->result->fsp_name->st.st_ex_mode;
 
-		if (!(S_ISREG(mode) || S_ISDIR(mode))) {
+		if (!(S_ISREG(mode) || S_ISDIR(mode) ||
+		      (S_ISLNK(mode) &&
+		       tc_client_resolves_links(state->result->conn)))) {
 			/*
 			 * Only open files and dirs without
 			 * FILE_OPEN_REPARSE_POINT
@@ -1818,7 +1820,8 @@ static void smbd_smb2_create_after_exec(struct tevent_req *req)
 	state->out_file_attributes = fdos_mode(state->result);
 
 	if ((state->out_file_attributes & FILE_ATTRIBUTE_REPARSE_POINT) &&
-	    (!(state->in_create_options & FILE_OPEN_REPARSE_POINT)))
+	    (!(state->in_create_options & FILE_OPEN_REPARSE_POINT)) &&
+	    !tc_client_resolves_links(conn))
 	{
 
 		uint32_t tag;
diff --git a/source3/smbd/smb2_read.c b/source3/smbd/smb2_read.c
--- a/source3/smbd/smb2_read.c
+++ b/source3/smbd/smb2_read.c
@@ -544,6 +544,28 @@ static struct tevent_req *smbd_smb2_read_send(TALLOC_CTX *mem_ctx,
 		return req;
 	}
 
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode) &&
+	    tc_client_resolves_links(fsp->conn)) {
+		/*
+		 * A Mac reading the data of a native link still has it
+		 * cached as the XSym file it created; see tc_native_links.c.
+		 */
+		if (!(fsp->access_mask & FILE_READ_DATA)) {
+			tevent_req_nterror(req, NT_STATUS_ACCESS_DENIED);
+			return tevent_req_post(req, ev);
+		}
+		status = tc_native_links_read_xsym(fsp,
+						   state,
+						   &state->out_data,
+						   (off_t)in_offset,
+						   (size_t)in_length);
+		if (tevent_req_nterror(req, status)) {
+			return tevent_req_post(req, ev);
+		}
+		tevent_req_done(req);
+		return tevent_req_post(req, ev);
+	}
+
 	if (!CHECK_READ_SMB2(fsp)) {
 		tevent_req_nterror(req, NT_STATUS_ACCESS_DENIED);
 		return tevent_req_post(req, ev);
diff --git a/source3/smbd/smb2_trans2.c b/source3/smbd/smb2_trans2.c
--- a/source3/smbd/smb2_trans2.c
+++ b/source3/smbd/smb2_trans2.c
@@ -68,7 +68,14 @@ bool refuse_symlink_fsp(const files_struct *fsp)
 		return true;
 	}
 	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode)) {
-		return true;
+		/*
+		 * A Time Capsule native link opened as itself has no fd;
+		 * the native HFS xattr backend acts on the link by path and
+		 * never follows it, so its own attributes are reachable,
+		 * as they are over AFP. Other backends fail without an fd.
+		 */
+		return !(fsp_get_pathref_fd(fsp) == -1 &&
+			 tc_native_links_enabled(fsp->conn));
 	}
 	if (fsp_get_pathref_fd(fsp) == -1) {
 		return true;
@@ -1517,11 +1524,14 @@ static NTSTATUS smbd_marshall_dir_entry(TALLOC_CTX *ctx,
 		SIVAL(p,0,mode); p += 4;
 		q = p; p += 4; /* q is placeholder for name length */
 		if (readdir_attr_data &&
-		    readdir_attr_data->type == RDATTR_AAPL) {
+		    readdir_attr_data->type == RDATTR_AAPL &&
+		    !(mode & FILE_ATTRIBUTE_REPARSE_POINT)) {
 			/*
 			 * OS X specific SMB2 extension negotiated via
 			 * AAPL create context: return max_access in
-			 * ea_size field.
+			 * ea_size field. The macOS client reads the
+			 * reparse tag from this field instead when the
+			 * entry is a reparse point, as with any server.
 			 */
 			ea_size = readdir_attr_data->attr_data.aapl.max_access;
 		} else {
@@ -2107,7 +2117,8 @@ NTSTATUS smbd_do_qfsinfo(struct smbXsrv_connection *xconn,
 
 			SIVAL(pdata,0,FILE_CASE_PRESERVED_NAMES|FILE_CASE_SENSITIVE_SEARCH|
 				FILE_SUPPORTS_OBJECT_IDS|FILE_UNICODE_ON_DISK|
-				conn->fs_capabilities); /* FS ATTRIBUTES */
+				conn->fs_capabilities|
+				tc_native_links_fs_capabilities(conn)); /* FS ATTRIBUTES */
 
 			SIVAL(pdata,4,255); /* Max filename component length */
 			/* NOTE! the fstype must *not* be null terminated or win98 won't recognise it
diff --git a/source3/smbd/smb2_write.c b/source3/smbd/smb2_write.c
--- a/source3/smbd/smb2_write.c
+++ b/source3/smbd/smb2_write.c
@@ -346,6 +346,25 @@ static struct tevent_req *smbd_smb2_write_send(TALLOC_CTX *mem_ctx,
 		return tevent_req_post(req, ev);
 	}
 
+	if (S_ISLNK(fsp->fsp_name->st.st_ex_mode) &&
+	    tc_client_resolves_links(fsp->conn)) {
+		/*
+		 * A Mac that just created a link may restate the XSym file it
+		 * wrote; the name is a native link by now. See
+		 * tc_native_links.c.
+		 */
+		status = tc_native_links_write_xsym(fsp,
+						    in_data.data,
+						    in_data.length,
+						    in_offset);
+		if (tevent_req_nterror(req, status)) {
+			return tevent_req_post(req, ev);
+		}
+		state->out_count = in_data.length;
+		tevent_req_done(req);
+		return tevent_req_post(req, ev);
+	}
+
 	if (state->in_offset == VFS_PWRITE_APPEND_OFFSET &&
 	    !fsp->fsp_flags.posix_append)
 	{
diff --git a/source3/smbd/vfs.c b/source3/smbd/vfs.c
--- a/source3/smbd/vfs.c
+++ b/source3/smbd/vfs.c
@@ -938,6 +938,12 @@ int vfs_stat(struct connection_struct *conn,
 	if (smb_fname->flags & SMB_FILENAME_POSIX_PATH) {
 		return SMB_VFS_LSTAT(conn, smb_fname);
 	}
+	if (VALID_STAT(smb_fname->st) &&
+	    S_ISLNK(smb_fname->st.st_ex_mode) &&
+	    tc_native_links_enabled(conn)) {
+		/* A native link handle refreshes the link, not its target. */
+		return SMB_VFS_LSTAT(conn, smb_fname);
+	}
 	return SMB_VFS_STAT(conn, smb_fname);
 }
 
@@ -979,7 +985,14 @@ NTSTATUS vfs_stat_fsp(files_struct *fsp)
 	}
 
 	if (fsp_get_pathref_fd(fsp) == -1) {
-		if (fsp->fsp_flags.posix_open) {
+		if (fsp->fsp_flags.posix_open ||
+		    (S_ISLNK(saved_stat.st_ex_mode) &&
+		     tc_native_links_enabled(fsp->conn))) {
+			/*
+			 * A native link opened as the object itself must
+			 * keep describing the link, never its target, so that
+			 * delete-on-close and rename still match its file id.
+			 */
 			ret = SMB_VFS_LSTAT(fsp->conn, fsp->fsp_name);
 		} else {
 			ret = SMB_VFS_STAT(fsp->conn, fsp->fsp_name);
@@ -1028,9 +1041,13 @@ NTSTATUS vfs_fstreaminfo(struct files_struct *fsp,
                 return NT_STATUS_OK;
         }
 
-	if (fsp_get_pathref_fd(fsp) == -1) {
+	if (fsp_get_pathref_fd(fsp) == -1 &&
+	    !(S_ISLNK(fsp->fsp_name->st.st_ex_mode) &&
+	      tc_native_links_enabled(fsp->conn))) {
 		/*
-		 * No streams on non-real files/directories.
+		 * No streams on non-real files/directories. A Time Capsule
+		 * native link has none of its own fd, but carries its own
+		 * attributes (see refuse_symlink_fsp()).
 		 */
 		return NT_STATUS_OK;
 	}
diff --git a/source3/wscript_build b/source3/wscript_build
--- a/source3/wscript_build
+++ b/source3/wscript_build
@@ -685,6 +685,7 @@ bld.SAMBA3_LIBRARY('smbd_base',
                           smbd/filename.c
                           smbd/open.c
                           smbd/close.c
+                          smbd/tc_native_links.c
                           smbd/blocking.c
                           smbd/sec_ctx.c
                           smbd/srvstr.c
