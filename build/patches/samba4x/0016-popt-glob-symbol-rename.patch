diff --git a/third_party/popt/poptconfig.c b/third_party/popt/poptconfig.c
index f0a92e0..f68d554 100644
--- a/third_party/popt/poptconfig.c
+++ b/third_party/popt/poptconfig.c
@@ -46,7 +46,7 @@ extern int glob_pattern_p (const char *__pattern, int __quote)
 /* Return nonzero if PATTERN contains any metacharacters.
    Metacharacters can be quoted with backslashes if QUOTE is nonzero.  */
 static int
-glob_pattern_p (const char * pattern, int quote)
+popt_glob_pattern_p (const char * pattern, int quote)
 	/*@*/
 {
     const char * p;
@@ -72,6 +72,7 @@ glob_pattern_p (const char * pattern, int quote)
     }
     return 0;
 }
+#define glob_pattern_p popt_glob_pattern_p
 #endif	/* !defined(__GLIBC__) */
 
 /*@unchecked@*/
